
1. Make R script to install dependencies in a local library if they aren't already.
2. DONE -- Implement an option in VetHybPiper.sh to skip collating and instead run individual
    blast searches for each target using only the contigs assembled for it.

